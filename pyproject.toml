[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scwire"
version = "0.1.0"
description = "Parse and visualize Spark Connect LoggingInterceptor logs"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
scwire = "scwire.cli:main"
